spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:default
    username: sa
    password:
  h2:
    console:
      enabled: true
custom:
  datasource:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health, appInfo